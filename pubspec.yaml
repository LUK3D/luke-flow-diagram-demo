name: luke_flow_diagram_demo
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter_web_plugins:
    sdk: flutter
  flutter:
    sdk: flutter
  go_router: ^16.0.0
  luke_flow_diagram: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
